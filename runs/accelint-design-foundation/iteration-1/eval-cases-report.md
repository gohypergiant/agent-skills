# accelint-design-foundation eval cases

Created `/Users/brandon.pierce/Projects/agent-skills/skills/accelint-design-foundation/evals/evals.json`.

Default eval coverage:
- CSS-module-first component styling in an existing design foundation app
- Vanilla Tailwind to design foundation migration
- Setup and build-error troubleshooting for missing `@reference` or PostCSS plugin config
- `data-*` plus `@variant` component variant styling
- Semantic spacing correction when numeric Tailwind assumptions leak in

Assessment:
- The set is realistic and aligned with the skill's actual intent.
- It covers both positive styling tasks and failure-diagnosis tasks.
- It should be sufficient as a default baseline for future manual or automated eval expansion.
