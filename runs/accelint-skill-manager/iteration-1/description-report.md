# Description Report

- Expanded trigger phrasing for underfiring maintenance cases such as "audit this skill package," "fix this SKILL.md," version/changelog alignment, and cross-file consistency review.
- Centered the activation boundary on the skill package itself to better separate this skill from prose-only, prompt-only, and README/docs work.
- Added explicit package artifacts such as `evals/` and `CHANGELOG.md` to improve routing for real maintenance tasks.
- Kept the negative boundary concise but firm for wording-only cleanup and user-facing docs requests.
