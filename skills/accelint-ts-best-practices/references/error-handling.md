# 3.3 Error Handling

Handle all errors explicitly. Do not swallow errors or rely on implicit failure paths. Use `try`/`catch`, Result-style returns, or another explicit error path that keeps failures visible to the caller.
